import { EthSdkConfig } from './types';
export declare function readConfig(filePath: string, requireJs: Require): Promise<EthSdkConfig>;
/** @internal */
export interface Require {
    (id: string): unknown;
    extensions?: {
        [K in string]?: (...args: any[]) => void;
    };
}
